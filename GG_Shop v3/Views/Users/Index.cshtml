@{
    ViewBag.Title = "User List";
    Layout = "~/Views/Shared/_Layout_admin.cshtml";
}
<div class="content-page">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <div class="d-flex flex-wrap align-items-center justify-content-between mb-4">
                    <div>
                        <h4 class="mb-0">Danh sách Người Dùng (Users)</h4>
                    </div>
                    <a href="@Url.Action("Create", "Users")" class="btn btn-primary add-list">
                        <i></i>Thêm Người dùng
                    </a>
                </div>
            </div>
            <div class="col-lg-12">
                <div class="table-responsive rounded mb-3">

                    <table id="datatable" class="data-table table mb-0 tbl-server-info">
                        <thead class="bg-white text-uppercase">
                            <tr class="ligth ligth-data">
                                <th>Tên tài khoản</th>
                                <th>Email</th>
                                <th>Tên người dùng</th>
                                <th>Số điện thoại</th>
                                <th>Trạng thái</th>
                                <th>Vai trò</th>
                                <th>Hành động</th>
                            </tr>
                        </thead>
                        <tbody class="ligth-body">
                        </tbody>
                </div>
            </div>
        </div>
    </div>



    @*pop-up ban user*@
    <div class="modal fade" id="BanModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-warning">
                    <h5 class="modal-title text-dark">Xác nhận xóa</h5>
                </div>
                <div class="modal-body">
                    Bạn có chắc muốn BAN người dùng này?
                </div>
                <div class="modal-footer">
                    <button class="btn btn-outline-warning mt-2" data-dismiss="modal">Hủy</button>
                    <button id="confirmBan" class="btn btn-warning mt-2">Xác nhận</button>
                </div>
            </div>
        </div>
    </div>

    @*pop-up unban user*@

    <div class="modal fade" id="UnbanModal" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-warning">
                    <h5 class="modal-title text-dark">Xác nhận mở khóa</h5>
                </div>
                <div class="modal-body">
                    Bạn có chắc muốn Unban người dùng này?
                </div>
                <div class="modal-footer">
                    <button class="btn btn-outline-warning mt-2" data-dismiss="modal">Hủy</button>
                    <button id="confirmUnban" class="btn btn-warning mt-2">Xác nhận</button>
                </div>
            </div>
        </div>
    </div>


</div>

@section scripts {
    <script>

    $(document).ready(function () {

        var UserId = 0;

    function loadUserTable(){
        $.ajax({
            url: '@Url.Action("getListUsers", "Users")',
            type: 'POST',
            success: function (rs) {
                var tbody = $("#datatable tbody");
                tbody.empty();

                rs.forEach(function (u) {
                    tbody.append(`
                    <tr>
                        <td>${u.Username}</td>
                        <td>${u.Email}</td>
                        <td>${u.Full_Name}</td>
                        <td>${u.Phone_Number}</td>
                        <td>${u.Status}</td>
                        <td>${u.Role}</td>
                        <td>
                            <div class="d-flex align-items-center list-action">
                                    <a class="badge badge-info mr-2" href="/Users/Details/${u.Id}">
                                        <i class="ri-eye-line"></i>
                                    </a>
                                    <a class="badge bg-success mr-2" href="/Users/Edit/${u.Id}">
                                        <i class="ri-pencil-line"></i>
                                    </a>
                                    <a class="badge bg-warning mr-2 btn-delete-user" data-id="${u.Id}">
                                        <i class="ri-delete-bin-line"></i>
                                    </a>
                                    <a class="badge badge-danger mr-2 btn-unlock-user" data-id="${u.Id}">
                                        <i class="ri-arrow-right-s-fill"></i>
                                    </a>
                              </div>
                        </td>
                    </tr>
                `);
                });
                $('#datatable').DataTable();
            },
            error: function () {
                alert('Lỗi hệ thống');
            }
        });
        };

    loadUserTable();

    // Khi nhấn nút XÓA
    $(document).on("click", ".btn-delete-user", function () {
        UserId = $(this).data("id");
        $("#BanModal").modal("show");
    });


    // Xác nhận xóa
    $("#confirmBan").click(function () {

        $.ajax({
            url: "/Users/BanUser",
            type: "POST",
            data: { id: UserId },
            success: function (rs) {
                $("#BanModal").modal("hide");
                alert(rs);

                // load lại bảng
                $("#datatable").DataTable().destroy();
                loadUserTable();
            },
            error: function () {
                alert("Lỗi hệ thống!");
            }
        });
    });


        $(document).on("click", ".btn-unlock-user", function () {
            UserId = $(this).data("id");
            $("#UnbanModal").modal("show");
        });
        $("#confirmUnban").click(function () {

            $.ajax({
                url: "/Users/UnbanUser",
                type: "POST",
                data: { id: UserId },
                success: function (rs) {
                    $("#UnbanModal").modal("hide");
                    alert(rs);

                    // load lại bảng
                    $("#datatable").DataTable().destroy();
                    loadUserTable();
                },
                error: function () {
                    alert("Lỗi hệ thống!");
                }
            });
        });


    });



    </script>
}
